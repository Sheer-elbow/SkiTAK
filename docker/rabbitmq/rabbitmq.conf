# Allow the default guest user to connect from other containers.
# OpenTAKServer builds its Socket.IO queue URL without credentials
# (amqp://<host>), which only works as guest/guest — see docker-compose.yml.
# The broker is only reachable on the internal Docker network.
loopback_users = none
